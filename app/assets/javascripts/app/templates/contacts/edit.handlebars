{{#with contact}}
  {{view Ember.TextField valueBinding="first_name"}}
  {{view Ember.TextField valueBinding="last_name"}}
  {{#if id}}
    {{submitButton "Update"}}
  {{else}}
    {{submitButton "Create"}}
  {{/if}}
{{/with}}
{{#view Ember.Link target="parentView" action="cancelForm"}}Cancel{{/view}}
